name: data_nexus
description: A production-grade data marketplace.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.0
  riverpod_annotation: ^2.3.0
  go_router: ^14.0.0
  http: ^1.2.0
  file_picker: ^8.0.0
  cross_file: ^0.3.3
  mime: ^1.0.4
  shared_preferences: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  riverpod_generator: ^2.4.0
  build_runner: ^2.4.0
  custom_lint: ^0.6.4

flutter:
  uses-material-design: true
